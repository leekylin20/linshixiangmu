# 安装测试日志

更新日期：2026-05-29

> 新装工具、依赖、运行时、CLI、缓存位置、测试是否跑通，都写这里。不要再只靠聊天记录和脑子记。

## 写入格式

| 日期 | 对象 | 动作 | 路径 | 结果 | 备注 |
|---|---|---|---|---|---|
| 2026-05-29 | 临时项目运行记忆层 | 创建防失忆/防垃圾堆入口 | `E:\临时项目\_索引` | 已创建 | 不移动、不删除现有项目 |

## 待回填

这些是从现有目录推断出的近期安装/测试痕迹，尚未逐项确认：

| 日期 | 对象 | 动作 | 路径 | 结果 | 备注 |
|---|---|---|---|---|---|
| 2026-05-29 | OpenStock | 新近创建/测试 | `E:\临时项目\OpenStock` | 待确认 | 需补用途和启动方式 |
| 2026-05-29 | BTC-Trading-Since-2020 | 新近创建/测试 | `E:\临时项目\BTC-Trading-Since-2020` | 待确认 | 需补用途和启动方式 |
| 2026-05-29 | female-photo-prompt-generator | 新近创建/测试 | `E:\临时项目\female-photo-prompt-generator` | 待确认 | 需补用途和启动方式 |
| 2026-05-28 | 飞书cli | 本地 CLI/缓存 | `E:\临时项目\飞书cli` | 已存在 | 与图明索同步相关 |
| 2026-05-28 | wechat_extract | 微信记录提取测试 | `E:\临时项目\wechat_extract` | 已存在 | 顶层有提取脚本 |
 
## 2026-05-30 新增安装记录

| 日期 | 对象 | 动作 | 路径 | 结果 | 备注 |
|---|---|---|---|---|---|
| 2026-05-30 | VoxCPM | 按 README 安装 PyPI 包、CUDA PyTorch、下载 VoxCPM2 模型并启动 Gradio | `E:\临时项目\VoxCPM`；缓存 `E:\临时项目\_system\hf-cache` | 已跑通 | `torch 2.11.0+cu128`；测试音频 `outputs\install_test.wav`；Web `http://127.0.0.1:8808` |
| 2026-05-30 | MoneyPrinterTurbo | 下载源码 zip、安装 uv/Python 3.11 环境、`uv sync --frozen`、启动 Streamlit | `E:\临时项目\MoneyPrinterTurbo` | 已跑通 | WebUI 改名“一键生成短视频”；Web `http://127.0.0.1:8501`；需补素材源/LLM API key 后做完整生成 |
| 2026-05-30 | FinceptTerminal | 克隆用户 fork，并用上游 v4.0.3 Windows 安装包安装可运行版 | 源码 `E:\临时项目\FinceptTerminal`；程序 `E:\临时项目\FinceptTerminalApp` | 已安装并启动 | fork 无 release；源码编译链缺 CMake/Ninja/MSVC/Qt；首次安装器误装到 `C:\Program Files\FinceptTerminal`，普通权限无法清理，需管理员权限后续处理 |
| 2026-05-30 | FinceptTerminal | 中文界面排查 | `E:\临时项目\FinceptTerminalApp\FinceptTerminal.exe`；源码 `E:\临时项目\FinceptTerminal` | 当前可运行包仍为英文 | 已写入 `general.language=zh_CN`，但 v4.0.3 exe 内无 `general.language`/`zh_CN`/`Interface language` 字符串，安装目录也无 `.qm` 翻译文件；说明当前上游发布包未包含多语言代码/资源。要中文需从 fork 源码重新构建带 i18n 的版本，或等待/制作新 release。 |
| 2026-05-30 | FinceptTerminal | 从 fork 源码构建中文资源版 | 源码 `E:\临时项目\FinceptTerminal\fincept-qt`；构建 `E:\fb\win-release`；运行版 `E:\临时项目\FinceptTerminalApp_zh` / ASCII 入口 `E:\FinceptTerminalApp_zh` | 已构建并启动 | 新版 exe 内含 `general.language`/`zh_CN`/`:/i18n/fincept_%1.qm`，日志确认 `Installed translator: zh_CN`。启动脚本：`E:\临时项目\FinceptTerminal\run_chinese_build.ps1`。为绕过网络/构建问题：使用 E 盘 Qt 6.8.3、OpenSSL 3.6.2、FetchContent 本地 zip、禁用可选 QXlsx/Excel export。 |

## 2026-05-31 新增安装记录

| 日期 | 对象 | 动作 | 路径 | 结果 | 备注 |
|---|---|---|---|---|---|
| 2026-05-31 | BiRefNet HR-matting | 克隆官方 GitHub 仓库并下载 HR-matting `.pth` 权重 | 源码 `E:\临时项目\BiRefNet`；权重 `E:\临时项目\BiRefNet\weights\BiRefNet_HR-matting-epoch_135.pth` | 已下载并校验大小 | 官方 Release 文件大小 `444622828` 字节；ModelScope/阿里镜像 `AI-ModelScope/BiRefNet` 可访问但未包含 HR-matting 权重 |
| 2026-05-31 | BiRefNet HR-matting | 建立项目内 `.venv`，安装 CUDA PyTorch/torchvision 与推理依赖，并新增单图抠图脚本 | 环境 `E:\临时项目\BiRefNet\.venv`；脚本 `E:\临时项目\BiRefNet\run_hr_matting.py` | 已跑通 | 测试命令输出 `outputs\demo\demo_shape_mask.png` 与 `outputs\demo\demo_shape_cutout.png`；GPU 为 RTX 4060 Laptop |
| 2026-05-31 | BiRefNet HR-matting Web UI | 安装 Gradio，新增本地网页操作界面并启动服务 | `E:\临时项目\BiRefNet\web_ui.py`；启动脚本 `E:\临时项目\BiRefNet\run_web_ui.ps1` | 已启动 | 本地地址 `http://127.0.0.1:7860`；页面已在 Codex 浏览器加载 |
| 2026-06-02 | female-portrait-director | 从 GitHub 安装 Codex Skill 到 E 盘技能目录 | `E:\临时项目\_system\codex-skills\female-portrait-director` | 已安装 | Git 直连 GitHub 超时，改用 codeload zip 下载；重启 Codex 后可用 `$female-portrait-director` 调用 |
| 2026-06-02 | Kronos | 克隆 GitHub 仓库，创建项目内 `.venv`，安装主依赖与 WebUI 依赖，预加载 Kronos-small | 源码 `E:\临时项目\Kronos`；环境 `E:\临时项目\Kronos\.venv`；缓存 `E:\临时项目\_system\hf-cache` | 已安装并启动 | WebUI `http://127.0.0.1:7070`；已通过接口加载 `Kronos-small (24.7M)` 到 `cuda:0` |
| 2026-06-05 | Muse-UI | 克隆 GitHub 仓库并完成本地 npm 依赖安装 | `E:\临时项目\muse-ui` | 已完成 | npm 缓存固定到 `E:\临时项目\_system\npm-cache`；使用 `npm install --legacy-peer-deps --no-audit --no-fund --ignore-scripts --prefer-offline`，Node 24 下可完成安装，但项目本身仍属 Vue2 老工具链 |
| 2026-06-06 | Muse-UI demo | 使用 Yarn Classic 重装依赖，下载 portable Node 10.24.1，并启动 Parcel demo | `E:\临时项目\muse-ui`；运行时 `E:\临时项目\_system\node-v10.24.1-win-x64` | 已跑通 | Web `http://127.0.0.1:8091/`；脚本 `E:\临时项目\muse-ui\run_demo.ps1`；Node 24 会卡在旧 `deasync@0.1.13` |
| 2026-06-06 | Muse-UI docs demo | 将本地 `demo/App.vue` 改为文档式使用界面，并清理项目内 Parcel `.cache` 后重启 `8091` 服务 | `E:\临时项目\muse-ui\demo\App.vue`；Web `http://127.0.0.1:8091/` | 已验证 | 页面包含 Overview / Installation / Usage / Theme / Components / Local Run；桌面和移动视口无横向溢出，Dialog 与 Snackbar 示例可交互，浏览器控制台无错误 |
| 2026-06-06 | muse-ui-fabric | 克隆真正的 Muse UI 画布工作台仓库，安装依赖，补 `run_workbench.ps1` 并启动 Vue CLI dev server | `E:\临时项目\muse-ui-fabric`；Web `http://127.0.0.1:8092/` | 已跑通 | 仓库 `zhouxianjun/muse-ui-fabric`；运行时 portable Node 10.24.1；页面已显示 Fabric 画布、图片对象、圆角/透明设置和工具栏。安装时 `canvas` 原生包提示缺 Python，但浏览器端工作台已成功编译运行 |
## 2026-06-07 video-skills-toolkit

- 目标：下载 Serena X 帖子提到的 6 个视频制作相关 GitHub Skills 仓库，并整理成本地工具包。
- 目录：`E:\临时项目\video-skills-toolkit`
- 下载方式：直连 GitHub 443 失败，改用 `https://gh-proxy.com/https://github.com/<owner>/<repo>/archive/refs/heads/main.zip`。
- 已下载：`heygen-com/hyperframes`、`browser-use/video-use`、`remotion-dev/skills`、`SamurAIGPT/Generative-Media-Skills`、`Ceeon/videocut-skills`、`dexhunter/seedance2-skill`。
- 扫描结果：共 85 个 `SKILL.md`，其中 Generative Media 60 个、HyperFrames 15 个、Remotion 1 个、Seedance2 2 个、video-use 2 个、videocut 5 个。
- 生成文件：`README.md`、`PROJECT.md`、`docs\INSTALLABLE_SKILLS.md`、`scripts\refresh-toolkit.ps1`、`scripts\install-local-skill.ps1`。
- 验证：两个脚本语法检查通过；用 `repos\seedance2-skill\zh` 做临时安装测试通过，测试目录已删除。

## 2026-06-07 BrowserAct Skills

- 目标：按截图中的 BrowserAct 技能页，把 `browser-act/skills` 下载到本地并整理成可复用工具包。
- 目录：`E:\临时项目\browseract-skills`
- 下载方式：直连 GitHub 443 失败，改用 `https://gh-proxy.com/https://github.com/browser-act/skills/archive/refs/heads/main.zip`。
- 扫描结果：共 59 个 `SKILL.md`，其中核心技能 2 个，solution 技能 57 个；分类为 ecommerce 12、lead-generation 9、search-research 5、social-listening 17、video-platforms 14。
- CLI 安装：使用 `E:\临时项目\_system\uv\uv.exe` 安装 `browser-act-cli==0.1.27`，并安装 uv 托管 Python `3.12.13` 到 `E:\临时项目\_system\uv-python`。
- 路径约束：`UV_TOOL_DIR`、`UV_TOOL_BIN_DIR`、`UV_CACHE_DIR`、`UV_PYTHON_INSTALL_DIR`、`BROWSERACT_DATA_DIR` 均固定到 `E:\临时项目\_system` 下。
- 已安装技能：`browser-act`、`browser-act-skill-forge` 已复制到 `E:\临时项目\.agents\skills`；重启 Codex 后生效。
- 验证：`browser-act --version` 返回 `browser-act 0.1.27`；`get-skills core --skill-version 2.0.2` 成功输出并保存到 `docs\get-skills-core-preview.txt`；`browser-act browser list` 返回 `No browsers found.`，说明 CLI 可运行但尚未创建浏览器身份。
- 后续：创建浏览器、导入 Chrome Profile、设置代理、登录/API Key 等属于敏感操作，按任务明确确认后再执行。

## 2026-06-07 serenity-skill

- 目标：安装 GitHub 仓库 `muxuuu/serenity-skill`，用于 Serenity / 白毛股神风格的产业链卡点投研。
- 安装方式：使用系统 `skill-installer` 脚本：`install-skill-from-github.py --repo muxuuu/serenity-skill --path . --dest E:\临时项目\.agents\skills --name serenity-skill --method auto`。
- 安装路径：`E:\临时项目\.agents\skills\serenity-skill`
- 内容：包含 `SKILL.md`、`references`、`assets`、`scripts`、`examples`、`agents`、README 和许可证等文件。
- 验证：`scripts\validate_skill.py` 返回 `OK: serenity-skill`；`scripts\serenity_scorecard.py --help` 正常输出参数说明。
- 同步：已运行 `E:\临时项目\skill-dashboard\refresh_skills.ps1`，技能看板与 `_索引\技能目录快照.md` 已更新，当前快照技能数 29。
- 风险边界：该 Skill 只做投研、排序和推理支持，不做买卖执行；涉及实时行情、公告、财报、监管文件、客户关系等必须联网核验。

## 2026-06-07 a-share-growth-scanner

- Target: create a reusable Codex skill for A-share growth screening that routes through local/structured data first, then filings/web evidence, then serenity-style thesis validation.
- Path: `E:\临时项目\.agents\skills\a-share-growth-scanner`
- Files: `SKILL.md`, `agents\openai.yaml`, `references\data-source-routing.md`, `scripts\eastmoney_quote.ps1`
- Validation: quote script tested with `600036,601088,000400,002028`; returned valid Eastmoney quote snapshots including price, PE, PB, market cap.
- Usage: trigger with requests like "扫描A股成长股", "找高回报A股候选", "用本地数据源筛选成长股", or explicit `$a-share-growth-scanner`.

## 2026-06-07 redbook 小红书 CLI

- 目标：为「图明索 P1 绿幕直播间落地解决方案」安装小红书选题/关键词/爆款拆解工具 `lucasygu/redbook`。
- 安装路径：`E:\临时项目\_system\redbook`
- npm 缓存/prefix：命令级固定为 `E:\临时项目\_system\npm-cache`、`E:\临时项目\_system\node-tools`。
- Wrapper：`E:\临时项目\_system\node-tools\redbook.ps1`、`E:\临时项目\_system\node-tools\redbook.cmd`。
- 验证：`redbook --version` 返回 `0.8.0`；`redbook search --help` 正常输出搜索参数。
- 构建说明：`npm run build` 中 `tsc` 已生成 `dist`，随后 Windows 缺少 `chmod` 导致脚本末尾失败；不影响 Windows 通过 `node dist\cli.js` / wrapper 运行。
- Cookie 状态：`redbook whoami` 可启动检测，但当前 Chrome Cookie 缺少 `web_session`；需关闭 Chrome 后重试，或手动传入 `--cookie-string "a1=...; web_session=..."`。
- 清理：`whoami` 过程中生成的 `C:\Users\麒麟\AppData\Local\Temp\redbook-*` 临时目录已清理；真实程序体和依赖保持在 E 盘。
