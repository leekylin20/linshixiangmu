# 从这里开始

这个目录已经重新梳理过：

1. 系统运行时在 `_system`。
2. 总索引在 `_索引`。
3. 项目目录暂时保留原位，避免破坏现有脚本路径。
4. 底层规则在 `AGENTS.md`：不要把程序实体、运行时、技能、缓存和临时依赖放到 C 盘；默认使用 `E:\临时项目\_system` 或项目内非 C 盘路径。

常用入口：

- `AGENTS.md`
- `_索引\README.md`
- `_system\agent-memory-lite\README.md`
- `_system\agent-memory-lite\scripts\Show-AgentMemoryBrief.ps1`
- `_system\agent-memory-lite\scripts\Search-AgentMemory.ps1 -Query "关键词"`
- `_索引\防失忆与防垃圾堆.md`
- `_索引\项目运行记忆.md`
- `_索引\安装测试日志.md`
- `_索引\清理候选.md`
- `_索引\项目分组.md`
- `_索引\顶层文件分组.md`
- `_索引\目录索引.csv`
- `_索引\顶层文件索引.csv`

新增规则：

- 新装工具、新建测试项目、跑通启动命令后，写入 `_索引\项目运行记忆.md` 或 `_索引\安装测试日志.md`。
- 可复用规则、启动命令、踩坑结论也写入 `_system\agent-memory-lite`，方便下次直接检索。
- 一次性测试输出、重复 zip、旧备份、空文件，先进 `_索引\清理候选.md`，不要直接删除。
- 这里不是第二大脑；业务知识和长期判断仍写入 `E:\obsidian`。
