# 低风险闭环样例：批量图片水印工具包

日期：2026-05-15  
工具 ID：`watermark-batch`  
状态：`verified`  
风险：`low`

## 1. 工具详情

```text
名称：批量图片水印工具包
路径：E:\临时项目\批量图片水印工具包
入口：E:\临时项目\批量图片水印工具包\一键加水印.bat
文档：E:\临时项目\水印工具包-归档日志.md
联网：否
secret/API key：否
输出：image / folder / zip
```

用途：

```text
Windows 可分发图片批量加水印工具，带 bat 入口、PowerShell 主程序、随包字体和 config.ini。
```

## 2. Codex 任务包

```text
# Codex 任务包：批量图片水印工具包

## 目标
对批量图片水印工具包做低风险闭环验证，不执行真实批处理，先确认入口、参数和审核边界。

## 允许范围
- 读取工具包 README、config.ini、bat、PowerShell 脚本和归档日志。
- 如需样例运行，必须使用明确的测试输入目录和新输出目录。
- 输出不得覆盖原图。

## 禁止事项
- 不处理用户未指定目录。
- 不递归处理全盘图片。
- 不上传图片。
- 不修改原始输入文件。

## 验收标准
- 入口文件存在。
- 配置文件存在。
- 输出目录规则清楚。
- 审核记录注明不覆盖原图和本地运行。
```

## 3. 运行记录模板

```text
# 工具运行记录

建议文件名：20260515-HHMMSS_watermark-batch_run.md
建议目录：E:\临时项目\toolbox-runs\

时间：待填写
工具：批量图片水印工具包
版本/路径：E:\临时项目\批量图片水印工具包
调用方式：一键加水印.bat 或命令行参数
运行状态：pending / success / partial / failed / blocked

## 输入

测试输入目录：待填写

## 输出

输出目录：待填写，必须是新目录。

## 参数

水印文字：
字体：
透明度：
位置：
递归：

## 日志摘要

待填写。不要粘贴用户隐私图片路径以外的敏感信息。

## 人工复核

□ 不覆盖原图
□ 输出目录明确
□ 字体加载正常
□ 中文水印无乱码
□ 输出图片可打开
```

## 4. Open Design Brief

```text
# Open Design Brief：批量图片水印工具包

## Surface
doc / prototype

## 目标
生成一个内部交付说明页，解释 Windows 用户如何解压、放入 input、双击 bat、查看 output。

## 必须体现
- input / output / config.ini / 一键加水印.bat 的关系。
- 默认不覆盖原图。
- 字体、透明度、位置、角度、递归等参数。
- 可分发 ZIP 和本地运行边界。

## 风格
工具说明页，清楚、可扫描，不做营销页。
```

## 5. 审核记录

```text
# 工具审核记录

工具名称：批量图片水印工具包
工具 ID：watermark-batch
路径：E:\临时项目\批量图片水印工具包
日期：2026-05-15

## 结论

状态：verified
风险等级：low

## P0

□ 本地路径存在：通过
□ 入口文件存在：通过
□ 不覆盖原文件：通过，归档日志已记录默认输出新目录
□ 不泄露 secret：通过，不需要 secret
□ 输出位置明确：通过，output / 时间戳目录

## P1

□ 有样例验证：通过，归档日志已有验证记录
□ 有依赖说明：通过，PowerShell + .NET System.Drawing + Noto Sans SC
□ 有已知边界：通过，本地 Windows 工具

## 下一步

可生成 Open Design 工具说明页，并作为第一个可交付样例。
```

## 6. 闭环结论

```text
工具详情 -> Codex 任务包 -> 运行记录模板 -> Open Design brief -> 审核记录
```

闭环已建立。当前未执行真实批处理、未处理用户图片、未上传。

