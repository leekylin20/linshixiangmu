# 审核修复记录

时间：2026-05-14 23:05 +08:00  
对象：个人工具箱工作台 v0.1 审核 Required Fixes

## 修复项

### 1. 静态服务器路径边界

文件：

```text
E:\临时项目\toolbox-dashboard\static-server.js
```

处理：

```text
把 target.startsWith(root) 改为 path.relative(root, target) 校验。
relative 结果不能以 .. 开头，也不能是绝对路径。
```

验证：

```text
http://127.0.0.1:8766/toolbox-dashboard/ -> 200
http://127.0.0.1:8766/%2e%2e/toolbox-manifest.json -> 403
http://127.0.0.1:8766/E:/临时项目-other/foo.txt -> 403
```

说明：

```text
http://127.0.0.1:8766/../toolbox-manifest.json 在客户端/HTTP 栈中会被规范化为根内 /toolbox-manifest.json，因此返回 200 不代表服务器路径越界。
```

### 2. API key / 外部服务风险上调

文件：

```text
E:\临时项目\toolbox-manifest.json
```

处理：

```text
livestream-marketing-image: risk_level medium -> high
file-to-md: risk_level medium -> high
prompt-optimizer: risk_level medium -> high
```

补充说明：

```text
上述工具涉及 API key、外部 AI provider、转写 API 或图像服务，v0.2 不接入真实参数表单，只保留任务包/审核文本。
```

### 3. 金融工具显式边界

处理：

```text
china-shenhua-backtest 的 run_hint 和 audit.notes 明确补充：
不实盘交易，不构成投资建议，不输出买卖建议。
```

### 4. photo-real-sim 状态修正

处理：

```text
photo-real-sim: status verified -> limited
```

原因：

```text
当前只验证了安装脚本复制流程，未验证 ComfyUI 节点实际运行。
```

## 修复后状态分布

```text
verified: 5
limited: 6
indexed: 6
candidate: 1
blocked: 1
```

## 修复后风险分布

```text
low: 4
medium: 5
high: 9
blocked: 1
```

## 页面复验

新端口：

```text
http://127.0.0.1:8766/toolbox-dashboard/
```

结果：

```text
工具卡片数：19
指标：总工具 19 / 已验证 5 / 有限可用 6 / 高风险与阻止 10 / 设计交付 19
默认详情：AI 内容工作流
控制台错误：0
```

截图：

```text
E:\临时项目\toolbox-artifacts\toolbox-dashboard-after-audit-fixes-20260514.png
```

## 结论

审核 Required Fixes 已处理。  
可以进入 v0.2，但仍按审核建议：先做运行记录生成器，不接真实执行。

