# 水印工具包归档日志

## 2026-05-08 归档记录

### 用户目标
- 设计一个 Windows 可复用自动化程序，用于给图片批量加文字水印。
- 水印文字：`图明索高清调试`
- 最终确认参数：
  - 字号：小四，等效 `12pt / 16px`
  - 字体：开源中文黑体 `Noto Sans SC`
  - 透明度：`80%`
  - 水印位置：图片中部，左侧 / 中间 / 右侧三处

### 产出文件
- 可复用工具包目录：`E:\临时项目\批量图片水印工具包`
- 可分发压缩包：`C:\Users\麒麟\Downloads\批量图片水印工具包_20260508.zip`
- 主入口：`E:\临时项目\批量图片水印工具包\一键加水印.bat`
- 配置文件：`E:\临时项目\批量图片水印工具包\config.ini`
- 处理脚本：`E:\临时项目\批量图片水印工具包\tools\watermark.ps1`
- 随包字体：`E:\临时项目\批量图片水印工具包\fonts\NotoSansSC-VF.ttf`
- 字体许可证：`E:\临时项目\批量图片水印工具包\fonts\LICENSE-NotoSansSC-OFL.txt`
- 使用说明：`E:\临时项目\批量图片水印工具包\README_使用说明.txt`

### 工具包结构
```text
批量图片水印工具包/
  一键加水印.bat
  config.ini
  README_使用说明.txt
  input/
  output/
  fonts/
    NotoSansSC-VF.ttf
    LICENSE-NotoSansSC-OFL.txt
  tools/
    watermark.ps1
```

### 默认配置
```ini
WatermarkText=图明索高清调试
InputPath=input
OutputPath=output
CreateTimestampFolder=true
Position=middle-sides
Opacity=80
FontSize=small4
Angle=-35
Recursive=false
JpegQuality=92
```

### 验证记录
- 验证 `tools\watermark.ps1` 可直接运行，能读取配置、加载随包 `Noto Sans SC` 字体并输出图片。
- 验证 `一键加水印.bat` 入口可运行，命令行模拟普通用户双击流程通过。
- 验证输出不会覆盖原图，默认输出到 `output\水印结果_时间戳`。
- 验证压缩包已生成，可发送给其他 Windows 用户解压使用。

### 使用方式
1. 解压 `批量图片水印工具包_20260508.zip`。
2. 把图片放入 `input` 文件夹。
3. 双击 `一键加水印.bat`。
4. 在 `output` 文件夹查看结果。

命令行用法：

```bat
一键加水印.bat "D:\图片目录"
一键加水印.bat "D:\图片目录" "D:\输出目录"
```

### 备注
- 不依赖 ImageMagick / ffmpeg。
- 依赖 Windows 自带 PowerShell 与 .NET `System.Drawing`。
- 字体为随包 `Noto Sans SC`，许可证为 Open Font License。
